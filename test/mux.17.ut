## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.k.uta"
CMPFILE="${srcdir}/mux.i.uta"
CMDFILE="${TS_TMPDIR}/cmdfile"

cat <<EOF > "${CMDFILE}"
"\${TOOL}" mux -f uta -o '${TS_OUTFILE}' '${INFILE}'
"\${TOOL}" print '${TS_OUTFILE}'
EOF
## STDIN

## STDOUT
TS_EXP_STDOUT="${CMPFILE}"

## mux.17.ut ends here
