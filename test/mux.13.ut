## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.h.dukas.bi5"
CMDFILE="${TS_TMPDIR}/cmdfile"

cat <<EOF > "${CMDFILE}"
TMPOUT='${TS_OUTFILE}'
"\${TOOL}" mux -f dukas --refdate 1328475600 --name 'AUDUSD' '${INFILE}' -o "\${TMPOUT}"
if test "${endian}" = "big"; then
	"\${TOOL}" fsck --little-endian "\${TMPOUT}" -o '${TS_OUTFILE}'
	rm -f -- "\${TMPOUT}"
fi
EOF

## STDIN

## STDOUT

## outfile reference
REFFILE="${srcdir}/mux.13.ref.ute"

## mux.13.ut ends here
