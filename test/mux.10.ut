## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.g.ibhist.xml"
CMDFILE="${TS_TMPDIR}/cmdfile"

cat <<EOF > "${CMDFILE}"
cat '${INFILE}' | "\${TOOL}" mux -f ibhist -o '${TS_OUTFILE}' -
if test "${endian}" = "big"; then
	"\${TOOL}" fsck --little-endian '${TS_OUTFILE}'
fi
EOF

## STDIN

## STDOUT

## reference
REFFILE="${srcdir}/mux.10.ref.ute"

## mux.10.ut ends here
