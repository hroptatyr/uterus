## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.i.uta"
CMDFILE=`mktemp "/tmp/tmp.XXXXXXXX"`

cat <<EOF > "${CMDFILE}"
cat '${INFILE}' | "\${TOOL}" mux -f uta -o '${TS_OUTFILE}' -
"\${TOOL}" print '${TS_OUTFILE}'
EOF
## STDIN

## STDOUT
stdout="${INFILE}"

## mux.15.ut ends here
