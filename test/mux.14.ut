## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.i.uta"
CMDFILE=`mktemp "/tmp/tmp.XXXXXXXX"`

cat <<EOF > "${CMDFILE}"
"\${TOOL}" mux -f uta '${INFILE}' -o '${TS_OUTFILE}'
"\${TOOL}" print '${TS_OUTFILE}'
EOF
## STDIN

## STDOUT
TS_EXP_STDOUT="${INFILE}"

## mux.14.ut ends here
