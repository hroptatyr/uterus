## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.g.ibhist.xml"
OUTFILE="testload.ute"
CMDFILE=$(mktemp "/tmp/tmp.XXXXXXXX")

cat <<EOF > "${CMDFILE}"
"\${TOOL}" mux -f ibhist '${INFILE}' -o '${OUTFILE}'
if test "${endian}" = "big"; then
	"\${TOOL}" fsck --little-endian '${OUTFILE}'
fi
EOF

## STDIN

## STDOUT

## reference file
REFFILE="mux.9.ref.ute"

## mux.9.ut ends here
