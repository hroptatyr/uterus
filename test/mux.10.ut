## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.g.ibhist.xml"
OUTFILE="testload.ute"
CMDFILE=$(mktemp "/tmp/tmp.XXXXXXXXXX")

cat <<EOF > "${CMDFILE}"
cat '${INFILE}' | "\${TOOL}" mux -f ibhist -o '${OUTFILE}' -
if test "${endian}" = "big"; then
	"\${TOOL}" fsck --little-endian '${OUTFILE}'
fi
EOF

## STDIN

## STDOUT

## reference
REFFILE="mux.10.ref.ute"

## mux.10.ut ends here
