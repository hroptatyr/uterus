## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.g.ibhist.xml"
CMDFILE=`mktemp "/tmp/tmp.XXXXXXXX"`

cat <<EOF > "${CMDFILE}"
"\${TOOL}" mux -f ibhist '${INFILE}' -o '${TS_OUTFILE}'
if test "${endian}" = "big"; then
	"\${TOOL}" fsck --little-endian '${TS_OUTFILE}'
fi
EOF

## STDIN

## STDOUT

## reference file
REFFILE="${srcdir}/mux.9.ref.ute"

## mux.9.ut ends here
