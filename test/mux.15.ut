## -*- shell-script -*-

TOOL=ute
INFILE="${srcdir}/mux.i.uta"
OUTFILE="testload.ute"
CMDFILE=`mktemp "/tmp/tmp.XXXXXXXX"`

cat <<EOF > "${CMDFILE}"
cat '${INFILE}' | "\${TOOL}" mux -f uta -o '${OUTFILE}' -
"\${TOOL}" print '${OUTFILE}'
EOF
## STDIN

## STDOUT
stdout="${INFILE}"

## mux.15.ut ends here
